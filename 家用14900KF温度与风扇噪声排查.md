# 家用 i9-14900KF 温度突升与风扇噪声排查

## 文档用途

回家后按本文逐步排查。下次开始新对话时，请先让助手阅读本文件，这样不需要重新说明前面的情况。

建议开场直接说：

> 请先阅读 `家用14900KF温度与风扇噪声排查.md`。我现在在家用电脑前，继续按文档排查。

## 已知情况

- 系统：Windows 10。
- 用途：运行 ComfyUI，也会有普通桌面负载。
- CPU：Intel Core i9-14900KF。
- 主板：技嘉 GIGABYTE Z790 UD。
- 散热：一体式水冷，具体品牌和型号暂时未知。
- BIOS 已更新到当时可用的最新版本。之前照片中显示过 BIOS F18a、Microcode 12F；回家后应重新确认当前版本。
- 旧照片曾显示 `Intel Default Settings: Extreme`，但后来已经修改过，因此不能把旧照片当作当前设置。
- 内存照片显示 64GB、DDR5-4800、XMP 关闭。这不是当前噪声问题的重点。
- Smart Fan 6 中：
  - `CPU_FAN` 约 1388 RPM，调成全速时噪声明显，大概率连接冷排风扇。
  - `CPU_OPT` 约 1735 RPM，改变策略时声音不明显，大概率连接水泵，但仍需确认接线。
  - `SYS_FAN1`、`SYS_FAN2A/B`、`SYS_FAN3_PUMP` 都显示 0 RPM，应当没有连接设备。
- 当前现象：经常只有一两个核心短时间高负载，CPU 温度快速上升，冷排风扇突然高速转动；数秒后负载和温度下降，风扇又安静，反复出现。
- 少量核心承担短时任务属于正常调度。需要解决的是频繁的温度尖峰和风扇响应，而不是强迫所有核心同时工作。
- 已尝试多种 BIOS 风扇曲线，改善不明显。
- Windows 电源计划已经是“平衡”，但最小处理器状态曾设为 20%，需要改为 5%。

## 重要安全规则

1. 每次只修改一类设置，修改前拍照，修改后记录结果。不要同时改 TVB、功耗、倍频和电压，否则无法判断是哪一项起作用。
2. 进入 BIOS 后先保存一个当前配置档案（如果 BIOS 支持），同时用手机拍下原值。
3. 不要通过把 `CPU_OPT` 转速设为 0 来判断它是不是水泵。水泵停转可能使 CPU 很快过热。
4. 不要把旧照片中的 `CPU 倍频调整 Auto 32` 直接改成 55。该选项不一定是单核最高睿频限制，误改可能变成全核高频，反而增加温度。
5. 不要设置固定 CPU 核心电压，不要关闭 C-State、Speed Shift，也不要一开始就关闭全部 Turbo Boost。
6. 降压后必须做稳定性测试。蓝屏、重启、程序退出、ComfyUI 无故报错或出现 WHEA 错误，都应视为不稳定。
7. 如果出现接近 100℃、Thermal Throttling、异常掉频、泵体异响或持续高温，应先停止压力测试并检查水冷安装，不要继续加压测试。

## 第一阶段：先做无需进入 BIOS 的检查

### 1. 修改 Windows 电源计划

路径通常为：

`控制面板 → 电源选项 → 更改计划设置 → 更改高级电源设置 → 处理器电源管理`

设置为：

- 电源计划：平衡。
- 最小处理器状态：`5%`。
- 最大处理器状态：`100%`。

不要把最大处理器状态长期设成 99%。99%通常会直接禁用睿频，虽然安静，但性能损失较大，只适合临时对照测试。

### 2. 下载 HWiNFO

HWiNFO64 是独立的硬件监控软件，个人非商业使用免费。建议下载 Portable 便携版，不需要安装。

- 官方下载页：<https://www.hwinfo.com/download/>
- 官方主页：<https://www.hwinfo.com/>

在下载页找到 `Portable`，选择正式稳定版的 `Free Download`。不要优先下载 Beta 测试版。

解压后运行 64 位程序，启动时选择：

`Sensors-only（仅传感器）`

HWiNFO 只用于读取数据，不会自动修改 BIOS、电压或频率。

### 3. 用 HWiNFO 记录一次噪声发生过程

1. 打开传感器窗口。
2. 找到 CPU 相关区域。
3. 点击底部的“重置最小值/最大值”按钮（通常是时钟形状）。
4. 正常使用电脑 10～20 分钟，尽量等风扇突然加速出现两三次。
5. 暂时不要运行专门的烤机软件，先记录日常状态。
6. 截图时保留 `Current、Minimum、Maximum、Average` 四列。

需要截图或记录以下项目；如果一张图放不下，可以分多张：

- `Core Clocks`：每个 P-core 的最高频率，重点看 Maximum。
- `Core Effective Clocks`。
- `Core VIDs`。
- `Vcore`。
- `VR VOUT`（如果主板提供；没有就不用找）。
- `CPU Package Temperature`。
- `Core Max Temperature` 或类似项目。
- `CPU Package Power`。
- `Thermal Throttling`。
- `Power Limit Exceeded` 或类似限制状态。
- WHEA 错误计数（如果 HWiNFO 显示）。

初步判断：

- `Core Clocks Maximum` 出现约 6000 MHz：TVB 确实触发，关闭 TVB 比较值得测试。
- 最高约 5800 MHz：主要是 Turbo Boost Max 3.0。
- 最高约 5700 MHz 或更低：关闭 TVB 可能只有有限改善。
- 短暂升温但最高温度低于约 85℃，并且没有 Thermal Throttling：通常是噪声控制问题，不表示散热器已经失效。
- 如果经常接近 100℃或出现 Thermal Throttling：需要优先检查水冷、硅脂、冷头压力和功耗设置。

注意：判断 TVB 应主要看 `Core Clocks` 的 Maximum。`Effective Clocks` 会受到采样时间和核心休眠影响，不一定能捕捉到完整的 6000 MHz 瞬时值。

### 4. 找出谁在周期性占用 CPU

噪声再次出现时打开：

`任务管理器 → 进程`

点击 CPU 列按占用率从高到低排列，记录当时排在最前面的进程名称并截图。需要区分：

- ComfyUI/Python 正常任务；
- 浏览器、Cursor或其他桌面程序；
- Windows Defender；
- Windows Search/索引；
- 更新程序或其他后台服务。

如果每次都是同一个后台程序触发，优先处理该程序，比限制整个 CPU 更合理。

## 第二阶段：回家后需要拍摄的照片

在修改 BIOS 前，把以下页面拍清楚。照片需要能看见选项名称和当前值：

1. BIOS Easy Mode 或“系统信息”页：
   - 主板型号；
   - BIOS 版本；
   - Microcode；
   - 当前 Intel Default Settings/PerfDrive 状态。
2. `频率/电压控制` 主页面。
3. 打开 `进阶处理器设置` 后，从顶部到底部逐页拍摄：
   - Intel Thermal Velocity Boost；
   - TVB Voltage Optimizations；
   - Intel Turbo Boost Technology；
   - Intel Turbo Boost Max Technology 3.0；
   - Active Turbo Ratios/Turbo Ratio Limits；
   - Turbo Power Limits；
   - PL1、PL2、Tau；
   - Current Limit/ICCMax。
4. 展开 `CPU/PCH Voltage Control` 后拍摄：
   - CPU 核心电压；
   - Dynamic Vcore (DVID)；
   - Load-Line Calibration/LLC（如果能看到）。
5. Smart Fan 6：
   - CPU_FAN 页面；
   - CPU_OPT 页面。
6. 水冷品牌和准确型号：
   - 水冷头或冷排上的型号标签；
   - 如果方便，拍主板上 CPU_FAN、CPU_OPT 附近的接线。

不要为了拍照先修改设置。先把照片发给助手确认具体选项，尤其是电压和倍频页面。

## 第三阶段：按顺序尝试 BIOS 设置

下面是候选方案，不要在第一次进 BIOS 时全部执行。每完成一个步骤，都回到 Windows 复测噪声和 HWiNFO 数据；问题已经达到可接受程度就停止继续修改。

### 方案 A：只关闭真正的 TVB

这是建议优先尝试的 CPU 侧调整，通常比手动改倍频简单。

在 `进阶处理器设置` 中寻找并区分：

- `Intel Thermal Velocity Boost`：设为 `Disabled`。
- `Intel Turbo Boost Technology`：保持 `Enabled`。
- `Intel Turbo Boost Max Technology 3.0`：先保持 `Enabled`。
- `TVB Voltage Optimizations`：保持 `Enabled` 或 `Auto`，不要因为名称中有 TVB 就一起关闭。它属于电压优化。

14900KF 的 TVB 会在温度、电流和功耗条件允许时，让少数优质 P 核短时间冲到约 6.0 GHz。关闭后通常仍可达到约 5.8 GHz，不等于关闭全部睿频。

预期影响：

- 单核极限性能可能下降约 2%～4%；
- 多核和主要由 GPU 承担的 ComfyUI 工作负载通常几乎无感；
- 瞬时频率、电压和温度尖峰可能减少，但如果原本就没有达到 6.0 GHz，改善可能有限。

修改后重复第一阶段的 HWiNFO 记录，比较：

- Core Clocks Maximum；
- Core VID/Vcore Maximum；
- CPU Package Temperature Maximum；
- 风扇突然加速的次数和持续时间。

### 方案 B：确认 Intel 默认档和主板增强功能

旧照片显示过 `Intel Default Settings: Extreme`，但用户已经修改过，因此以当前 BIOS 照片为准。

通常目标是：

- Intel Default Settings：优先选择 `Performance`，而不是 Extreme。
- GIGABYTE PerfDrive：使用 Intel Default/Optimization 一类保守档位。
- `Unleash`：关闭。
- `Instant 6GHz+`：关闭。
- `Spec Enhance`：关闭。
- 主板多核增强/Multi-Core Enhancement：关闭或 Intel Default。

具体名称和可选值必须根据新照片确认。

### 方案 C：设置功耗限制

如果持续多核负载温度高或冷排风扇长期很吵，可以尝试：

- PL1：`150 W`。
- PL2：`200 W`。
- Tau：`56 秒`。

功耗限制主要改善持续多核负载。单个核心几秒钟的频率尖峰通常低于 PL2，所以它对当前“瞬间加速”的效果可能不如关闭 TVB 或降压明显。

不要在没确认选项名称时，把 TDP、Current Limit、PL1 和 PL2 混着填写。

### 方案 D：小幅负电压偏移

只有在看过当前 BIOS 电压页面并确认修改方法后再进行。

技嘉 BIOS 常见方式可能是：

- CPU 核心电压：`Normal` 或适当的 Adaptive 模式；
- Dynamic Vcore (DVID)：先从 `-0.030 V` 开始；
- LLC：保持 `Auto` 或 `Normal`。

如果稳定且温度仍需改善，再考虑逐步到 `-0.040 V`、`-0.050 V`。不要直接使用很大的负偏移。

注意：不同 BIOS 版本中 `Auto`、`Normal`、Adaptive 和 DVID 的联动方式可能不同，所以在助手确认页面之前不要照抄修改。

#### 降压后的稳定性测试

可选测试软件：

- OCCT 官方下载：<https://www.ocbase.com/download>

建议至少完成：

1. OCCT CPU 测试 30 分钟。
2. 正常运行常用 ComfyUI 工作流。
3. 待机一段时间，再进行睡眠唤醒和重启。
4. 检查 HWiNFO 的 WHEA 错误，以及 Windows 事件查看器中的 WHEA-Logger。

降压不稳定不一定只在满载时出现；轻载、单核睿频、待机切换和睡眠唤醒也可能报错。

如果出现错误，将 DVID 向 0 调回。例如 `-0.050 V` 不稳定，就改回 `-0.040 V` 或 `-0.030 V`。

### 方案 E：进一步限制单核最高睿频

这是关闭 TVB 仍无效时的后续方案。必须先拍 `Active Turbo Ratios/Turbo Ratio Limits` 页面，由助手确认哪个字段控制 1～2 个活跃 P 核的最高倍频。

不要修改旧照片中的 `CPU 倍频调整 Auto 32`。

目标可能是把 1～2 核最高睿频逐级限制到 5.7～5.5 GHz，但应根据实际 BIOS 字段填写，不能在未确认菜单时直接操作。

## 水冷与风扇控制

### 接口的当前推断

- CPU_FAN：大概率是冷排风扇。
- CPU_OPT：大概率是水泵。
- SYS_FAN：没有接设备。

这只是根据声音和 RPM 作出的推断。最可靠的方法是查看水冷说明书或顺着线材确认。

如果确认 CPU_OPT 是水泵：

- 水泵应保持稳定转速，不要跟随 CPU 瞬时温度频繁变速。
- 如果全速没有明显泵噪，可以设为 Full Speed。
- 如果全速产生共振，可考虑固定在约 80%～100%之间，但应参考水冷厂商说明。
- 可开启 CPU_OPT 转速故障警告。

CPU_FAN 冷排风扇才使用温度曲线。之前讨论过的参考曲线为：

- 20℃：25%；
- 30℃：25%；
- 40℃：30%；
- 50℃：35%；
- 60℃：45%；
- 70℃：60%；
- 85℃：100%；
- Temperature Interval：如果最大只有 3，就保持 3；
- Fan Mode：Slope。

但用户已经尝试过多种风扇策略，改善不明显，因此后续重点是 HWiNFO 数据、TVB和电压，而不是继续盲调曲线。

### 更理想的冷排风扇控制方式

CPU 核心温度会在一两秒内大幅变化，但冷却液温度变化很慢。冷排风扇如果直接跟随 CPU Package 温度，就容易忽快忽慢。

优先级如下：

1. 如果水冷带 USB 控制和冷却液温度传感器：使用水冷厂商软件，让冷排风扇跟随冷却液温度。
2. 如果没有水温传感器：可以考虑 Windows 下使用 Fan Control，对 CPU 温度做平均或延迟响应。

Fan Control 官方发布页：

<https://github.com/Rem0o/FanControl.Releases>

在没有确认水冷型号、风扇接口和软件控制能力前，不必先安装 Fan Control。部分主板控制与第三方风扇软件可能互相覆盖，使用前应先确认。

## 下次对话首先提供什么

建议按以下顺序发送：

1. HWiNFO 日常使用 10～20 分钟后的传感器截图。
2. 风扇突然加速时，任务管理器 CPU 排序截图。
3. 水冷品牌和准确型号。
4. BIOS `进阶处理器设置` 全部页面照片。
5. BIOS CPU 电压页面照片。

下一次应先根据 HWiNFO 判断问题属于哪一类：

- 后台程序周期性单核占用；
- TVB 6.0 GHz 电压尖峰；
- 普通 5.7～5.8 GHz 睿频尖峰；
- 主板功耗/增强策略过于激进；
- 核心电压偏高；
- 水冷或安装导致的持续高温。

在拿到数据前，最安全的操作只有：

1. Windows 最小处理器状态改为 5%。
2. 安装或运行 HWiNFO Portable。
3. 收集截图。
4. 确认水冷型号和接线。

不要在同一轮里直接把所有 BIOS 候选设置全部修改。
